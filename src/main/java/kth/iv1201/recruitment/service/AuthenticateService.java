package kth.iv1201.recruitment.service;

import kth.iv1201.recruitment.entity.UserForm;
import org.springframework.stereotype.Service;

@Service
public class AuthenticateService {
	public int authenticate(UserForm form) {
		// Person person = personDAO.authenticate(form.getUsername(), form.getPassword());
		// return person;
		// TODO send information to backend service.
		// TODO 'TRUE' return authenticated user.
		// TODO 'FALSE' return failed authentication.
		return 0;
	}
}
